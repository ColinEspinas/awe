import { OuterNode } from './OuterNode';

/**
 * A system is an outer node that can be globally registered to the engine.
 *
 * This allows global usage of functionalities like rendering, physics, inputs, etc.
 */
export class System extends OuterNode {

}
