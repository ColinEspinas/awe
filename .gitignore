# Modules added by npm.
node_modules

# ESLint cache
.eslintcache

# Distribution and Documentation directories are generated by CI/CD
# so we don't need to push them.
dist
docs

# Made for testing and development purposes,
# this directory is where you'll want to scratch around with ineka.
sandbox